# Postpin — Build Tracker (living)

> Single source of truth for what is **real vs mock**. A flow is 🟢 only when its backend
> endpoint is wired **and** its frontend page consumes it **and** its test/verify gate passes
> **and** the page imports nothing from `src/lib/mock-data.ts`.
>
> Status: 🔴 todo · 🟡 in progress · 🟢 done · ⏸ blocked

**Last updated:** 2026-07-04 · **Current milestone:** ALL SHIPPED (M0–M7 + admin deferrals + gap-fix sweep) · **Overall:** M0–M7 ✅ · backend **150/150** vitest · zero mock-data imports · zero "coming soon" UI

## M2b — Dashboard + usage reads ✅ VERIFIED
- Backend: `GET /v1/subscription` (plan + real quota from apiLogs since period start), `GET /v1/usage/{summary,series,logs,endpoints,status}` (apiLogs aggregation). Enriched apiLogs with `detail` {origin,destination,zone}. `services/usage.service.ts` + `routes/{usage,subscription}.routes.ts`. **30/30 vitest** (M2b: 2).
- Frontend: `app/page.tsx` dashboard + `app/usage/page.tsx` rewritten to react-query hooks (`lib/api/services/{usage,subscription}.ts`), off `currentUser`/`usageSeries`/`recentCalls`/`endpointStats`/`statusBreakdown` mock. **Real CSV export** (client-side blob). Browser-verified: dashboard shows Free · 3/1000 · 997 left; usage shows 3 calls · 100% · /v1/rates/calculate 93ms.

## M2 — Rate engine + public API + keys

| Flow | Backend | Frontend | Tested? | Status |
|---|---|---|---|---|
| Server rate engine (paise) | `services/rate-engine.service.ts` | — | vitest ✅ | 🟢 |
| Public rate/serviceability/pincode/plans | `routes/public.routes.js` (`/v1/public/*`, IP rate-limited) | rate-calculator + (pricing pending) | vitest ✅ + live curl ✅ | 🟢 BE; calc wired |
| API keys CRUD | `routes/keys.routes.js` (`/v1/keys`) + `api-key.service` | `app/keys/page.tsx` (react-query) | vitest ✅ | 🟢 |
| Key-auth + keyed rate API + Redis limit/quota + apiLogs | `middleware/api-key-auth.js` + `routes/rates.routes.js` | — | vitest ✅ (create→use→revoke→401) | 🟢 |
| Rate calculator → public API | — | `components/shipping/rate-calculator.tsx` | typecheck ✅ | 🟢 |
| Fix `/legal/sla` dead link | — | `status/page.tsx` → `/legal/terms#sla` | ✅ | 🟢 |
| `/pricing` → `/v1/public/plans` | ✅ endpoint | wired via react-query (comparison table from live plans) | ✅ | 🟢 |
| Keys detail `/app/keys/[id]` | `GET /v1/keys/:id` + per-key logs | fully wired (react-query) | browser ✅ | 🟢 |
| Dashboard/usage reads (apiLogs aggregation) | `/v1/usage/*` + `/v1/subscription` | dashboard + usage pages live | 30/30 + browser ✅ | 🟢 (see M2b above) |

**M2 gate: ✅ 28/28 vitest + ✅ browser E2E 6/6** — anon landing quote (public API); login; create key (UI, one-time `pp_test_` secret); keyed `/v1/rates/calculate` → ₹196.66; revoke → 401; no-key → 401. Robustness fix: empty-body JSON POST → `{}` (no more spurious 400 on `/keys/:id/revoke`).
**Remaining M2:** none — the three residuals above were closed in M2b (2026-07-01) and the 2026-07-03 sweeps.

---

## Kanban

- **Done:** M0 foundation · M1 auth+tenancy · M2/M2b rate engine + public API + keys + usage · M3 webhooks/rate-cards · M4/M4b Razorpay billing + coupons/PDF/dunning/refunds · M5 support/notifications/account/team · M6a–f admin portal (incl. zones editor, rate-cards admin, plan create, notification channels, staff invites, 2FA, impersonation) · M7 public surface · nightly India Post sync · gap-fix sweep (webhook events, subscription lifecycle, engine.defaults wiring, honest usage, contact form, tenant 2FA UI)
- **Doing:** —
- **Next:** Ops hardening (PM2, real SMTP, Razorpay live keys, backups) — tracked below as the only open work
- **Left:** —

---

## Blockers register

| # | Blocker | Affects | Status |
|---|---|---|---|
| 1 | Mongo is **standalone** (SSH tunnel to remote) → no multi-doc transactions | M1 onboarding | Mitigated — onboarding uses compensating cleanup, not a transaction |
| 2 | Shared `cnc_db` is another app's prod data | all writes | Resolved — using dedicated `postpin` db |
| 3 | Web `node_modules` was incomplete (`@swc/helpers`) | running web app | Resolved — web builds/runs; many Playwright E2Es since |
| 4 | **Redis** required from M2 (rate-limit/quota/cache) | M2+ | Resolved — Docker `postpin-redis` on :6380, restart=unless-stopped |
| 5 | **SMTP** creds for real email | prod email delivery | OPEN (ops) — dev captures locally; prod needs real creds |
| 6 | **Razorpay** test keys | M4 billing | Resolved — valid test keys in `server/.env`; live keys = ops item |
| 7 | SSH tunnel must be up for the API to reach Mongo | run/seed | Operational — confirm tunnel before `npm run dev`/seed |

---

## M0 — Foundation ✅

| Item | Status | Notes |
|---|---|---|
| `server/` Fastify scaffold + deps | 🟢 | boots on :4000; helmet/cors(credentials)/cookie/rate-limit/zod |
| `/health` + JWKS (EdDSA) | 🟢 | verified live; db:connected to `postpin` |
| Mongo connection (`directConnection`) | 🟢 | dedicated `postpin` db |
| 22 Mongoose models + `companyId`-leading indexes | 🟢 | `registerIndexes()` built live; `assertScoped()` enforces invariant |
| RBAC contracts (`shared/permissions.ts`, `roles.ts`) | 🟢 | permission catalog + system role bundles |
| Tenancy primitives (context, scoped-repo, admin-repo, audit) | 🟢 | injection-wins + 404-before-403 + tamper audit |
| Test gate (vitest + mongodb-memory-server) | 🟢 | 10/10: health, jwks, 404 envelope, assertScoped, isolation proofs |
| Frontend `src/lib/api` client + `<SessionProvider>` + `QueryBoundary` (no edge middleware — client-side gating by design) | 🟢 | completed + E2E-verified in M1 |

---

## M1 — Auth + Tenancy (current)

| Flow | Backend endpoint(s) | Frontend wiring | Tested? | Status |
|---|---|---|---|---|
| Seed (permissions/roles/plans/settings/super-admin/zones/pincodes/demo tenant) | `scripts/seed.ts` | — | vitest + live run ✅ (2026-06-30) | 🟢 |
| Signup → company+owner+roles+Free-sub (+verify email) | `POST /v1/auth/signup` | `(auth)/signup` | integration ✅ | 🟢 done (E2E) |
| Verify email | `POST /v1/auth/verify-email` | `(auth)/verify-email` | integration ✅ | 🟢 done (E2E) |
| Login (lockout 423, Argon2id) | `POST /v1/auth/login` | `(auth)/login` | integration ✅ | 🟢 done (E2E) |
| Refresh rotation + reuse→family revoke | `POST /v1/auth/refresh` | api client single-flight | integration ✅ | 🟢 done (E2E) |
| Logout / logout-everywhere | `POST /v1/auth/logout(-all)` | shell user menu | integration ✅ | 🟢 done (E2E) |
| Forgot / reset password (SMTP capture) | `POST /v1/auth/forgot-password`,`/reset-password` | `(auth)/forgot`,`/reset` | integration ✅ | 🟢 done (E2E) |
| Me / session context | `GET /v1/auth/me` | `<SessionProvider>` + app/admin layouts | integration ✅ | 🟢 done (E2E) |
| RBAC (member 403) + permVersion → token_stale | middleware authorize/authenticate | `can()` gating | integration ✅ | 🟢 BE |
| **Tenant isolation (HTTP)** (A gets 404 on B; injection-wins) | scoped-repo + `/v1/_demo` | (demo) | integration ✅ | 🟢 BE |
| Remove OAuth buttons | — | `(auth)/login`,`/signup` | render smoke ✅ | 🟢 |
| Client-side auth gating `/app` + `/admin` | `GET /v1/auth/me` (+refresh bootstrap) | `portal-shell`/`admin-shell` + `<SessionProvider>` | E2E ✅ (logged-out /app→/login) | 🟢 |
| Frontend API client (credentials, single-flight 401→refresh, CSRF, error envelope) | — | `src/lib/api/{client,errors,services/*}` | E2E ✅ | 🟢 |
| Wire 5 (auth) pages to real endpoints | `/v1/auth/*` | `(auth)/{login,signup,forgot-password,reset-password,verify-email}` | **E2E 6/6 ✅** | 🟢 |
| Portal/admin shells off `currentUser` mock | `GET /v1/auth/me` | `app/layout`,`admin/layout` → shells | E2E ✅ (real "Aarav Sharma" + Free plan in shell) | 🟢 |
| Rate-limit 429 on auth | `@fastify/rate-limit` | — | live-verified 2026-07-04 (10×401 → 429 on /auth/login) | 🟢 |

> **Architecture note:** gating is **client-side** (SessionProvider), not Next edge middleware — the access token is in-memory and the API is a separate origin, so the edge can't read it. Bootstrap = `/me` → on 401 single-flight `/auth/refresh` (refresh cookie) → retry. No `src/middleware.ts`.
> **Residual mock:** none — signup plan chip moved to `/v1/public/plans` in M2; `src/lib/mock-data.ts` itself was deleted on 2026-07-04.

**M1 backend gate: ✅ 21/21 vitest** (signup→verify→login→/me→logout; isolation 404 + injection-wins; refresh-reuse family revoke; member 403; permVersion→token_stale; lockout; non-enumerating forgot; dup-signup 409; onboarding shape). **Remaining M1:** none — live seed, api-client, SessionProvider, all 5 (auth) pages, OAuth removal and the Playwright E2E all closed on 2026-06-30 (changelog entry 4).

---

## M2–M6 (summary; expanded when reached)

- **M2 Rate engine + public API + keys:** `/v1/rates/{calculate,bulk}`, `/serviceability`, `/pincodes`, keys CRUD + key-auth + Redis rate-limit/quota; wire marketing + playground calculators, dashboard/usage/keys reads. Fix `/legal/sla`, blog links.
- **M3 Developer writes:** keys detail/domains, webhooks CRUD+deliveries+replay, rate-cards edit.
- **M4 Billing:** Razorpay order→checkout→verify, invoices+PDF, coupons, admin billing/refunds; invoice/dunning email.
- **M5 Support/notifications/settings:** tickets+replies, notifications feed, profile/team/security; ticket/notification email.
- **M6 Admin portal (~18 pages):** tenants/users (suspend/impersonate+step-up), plans/coupons, usage reports, pincodes (sync+rollback), zones, rate-cards, audit logs, settings; cross-tenant isolation hardening.

---

## Changelog

- **2026-07-06** — **Repo restructured into two top-level apps: `frontend/` (Next.js — src, public, all configs, node_modules, .env.local) and `server/` (Fastify API).** All 154 web files moved via git rename (history preserved; staged, not committed). Root now holds only `frontend/`, `server/`, `docs/`. Dev commands: web `cd frontend && npm run dev` (:3000), api `cd server && npm run dev` (:4000). Windows note: VSCode-held directory handles blocked plain renames of `src/app`+`src/components` — files were relocated with `robocopy /MOVE` (delete allowed where rename wasn't); web typecheck + dev boot re-verified from the new location.

- **2026-07-04 (2)** — **Gap-fix sweep: the 46-finding audit's functional gaps + every remaining dummy closed. Backend 150/150.** (a) **Outbound webhooks are real**: `emitWebhookEvent`/`emitWebhookEventToAll` (context-free, 3 attempts w/ backoff, per-attempt delivery records) fired from key.created/key.revoked (api-key.service), invoice.paid + subscription.updated (billing activate/cancel), sync.completed/failed (pincode sync broadcast), rate.calculated (keyed rates route, fire-and-forget) — signed payloads, secrets never included. (b) **Subscription lifecycle exists**: `subscription-lifecycle.service.ts` hourly sweeper — free periods roll forward, `cancelAtPeriodEnd` downgrades to Free at period end, expired paid plans get ONE renewal notice (email+notification) in a 7-day grace then downgrade; audits `billing.downgraded_on_{cancel,expiry}`. (c) **engine.defaults is live**: DB-backed cached getter in the engine (quotes, card simulate, checkout GST, tenant rate-card surcharge display); PATCH invalidates; breakdown hints computed from bps. (d) **Quota warning** at `company.quotaWarningPct` (default 80%) — Redis SETNX-idempotent, once per period, notifies the owner. (e) **Honest admin usage**: tenant-detail + usage-report quota now count real apiLogs in the current period (dead `usage.callsUsed` no longer rendered). (f) **security.alert dispatches** on lockout + impersonation; billing.payment.failed alert on dunning. (g) **Failed keyed calls now logged** to apiLogs with real status. (h) **Contact form is real**: `POST /v1/public/contact` (rate-limited 5/min) → ops inbox (notification-config recipients, fallback seed admin) + audit; frontend wired with honeypot kept. (i) **Tenant 2FA UI shipped** — shared `TwoFactorCard` on `/app/settings/security` (fake "coming soon" toggle deleted); same component powers admin "Your 2FA". (j) **Marketing de-dummied**: footer status pill reads live /public/status; hero Send button fires a REAL /v1/public/rates call and re-renders the panel from the response (live engine-ms pill); landing stats fetch live pincode count (RSC, 1h revalidate); "one-click rollback" claim removed; blog links point to docs; `mock-data.ts` deleted. (k) sync-settings: retries/timeoutMs now honored by the live sync fetcher; final 2 "coming soon" notes rewritten. (l) Webhook endpoint cap served by the API (`GET /v1/webhooks` → `cap`), client fallback only. (m) **Pricing-authority lock**: tenants can no longer edit/delete the platform-ASSIGNED billing card (403 `card_platform_managed`; drafts stay editable) — closes the hole where a tenant could rewrite their own charged slabs. Auth 429 live-verified (10×401→429). Tests: `m6f.gap-fixes.test.ts` +11 (webhook deliver+sign+retry, sweeper cancel/grace/rollover, engine.defaults repricing, contact, honest usage, cap, pricing lock) → **150/150**. Known non-blockers intentionally deferred to ops: overage billing, GST invoice profile, shipping rules, audit export/tenant delete/sync rollback endpoints, webhook secret encryption-at-rest, email retry queue, notification email fan-out/templates.

- **2026-07-04 (7)** — **ALL deferred admin features shipped + media uploads + sync UX hardening. Backend 139/139 vitest; web + server typecheck clean.** Nothing dummy/non-functional remains. New tests: `m6d` (+2 → 16), `m6e.admin-features` (19), `m6f.media` (4).
  - **Sync UX hardening:** durable `GET /admin/pincodes/sync/status` (running/current/last, 15-min stale-lock guard) + single-flight `POST /admin/pincodes/sync` now 409s `sync_in_progress`; frontend "Run live sync" button polls status (3s while running), stays disabled until complete/failed, toasts the outcome. Sync-fail now fans out to platform alert channels.
  - **Plan create:** `POST /admin/plans` (`adminCreatePlan`, slug validation, rupees→paise) + "New plan" dialog reusing the editor.
  - **Zones editor:** added `baseChargePaise`/`perKgPaise` to the Zone model + a **DB-backed cached pricing resolver** in the rate engine (`getZonePricing`, `invalidateZonePricing`), so editing a zone (`PATCH /admin/zones/:code`) **reprices live quotes** (fallback to the built-in seed keeps existing quotes identical). Full zone edit dialog.
  - **Platform settings:** made arrays/null/nested values editable via validated JSON fields (the API already deep-merged); dropped the "coming soon" note.
  - **Notifications config:** `services/platform-alerts.service.ts` — real `dispatchPlatformAlert` to **email (nodemailer) + Slack (webhook)** gated by per-channel min-severity + per-event toggles; `GET/PATCH /admin/notifications/config` + `POST /admin/notifications/test` (delivers a real test alert). Wired into the pincode-sync failure path. Channels config card on the admin Notifications page.
  - **Rate-cards admin (create/edit/assign/simulate):** cross-tenant `POST/PATCH/GET /admin/rate-cards`, `POST …/assign` (active+default, demotes siblings), `POST …/simulate` (real slab math). **Assign genuinely reprices keyed API calls** — the engine loads a company's active default card (cached, `companyId` threaded from the keyed route) and prices from its slabs (`cardFreightPaise`), fallback to zone pricing. Full page: create/edit dialog (tenant picker + per-zone base/step), Assign, and a Simulate dialog.
  - **Team/staff:** `POST /admin/team/invite` (real invite email → existing accept-invite flow), `POST /admin/team/:id/remove` (last-super-admin guard + session revoke), `GET /admin/roles/matrix` (live role×permission grid). **2FA/TOTP** end-to-end: `services/mfa.service.ts` (AES-256-GCM encrypted secret, otplib, one-time backup codes, replay guard) + `/auth/2fa/{status,setup,enable,disable}` + **two-step login** (`/auth/login` returns an MFA challenge → `/auth/login/2fa`). Login page handles the 2FA step; team page gained Invite dialog, a **Your 2FA** tab (QR enroll + backup codes), the permission matrix, and per-row remove.
  - **Media uploads (local disk):** `@fastify/multipart` + `@fastify/static` at `/uploads/`, `services/upload.service.ts` (type/size validation, path-traversal-safe delete). **Avatars:** `POST/DELETE /auth/avatar`, `avatarUrl` on the user + `/me`; the profile "Upload photo" button is now a **real file upload** (was a fake toast). **Ticket attachments:** `POST /tickets/uploads` + `attachments[]` on create/reply; new-ticket compose and the reply thread upload real files and render them. `apiFetch` now passes `FormData` through untouched.
  - **Tenant impersonation:** `POST /auth/step-up` (password + 2FA re-auth → short-lived step-up token) → `POST /admin/tenants/:id/impersonate` (admin:write) issues an **access-token-only** session acting as the tenant owner (`act` claim records the real admin; critical audit). Admin's refresh cookie is untouched, so **Exit just refreshes back to the admin**. Impersonate dialog on the tenant detail page + a sticky amber banner in the tenant portal with Exit.
  - **Verify note:** backend fully test-verified (app.inject drives the real app); live browser E2E deferred — needs the user's SSH tunnel (27017) + dev servers up.

- **2026-07-03 (6)** — **Nightly auto-sync of the India Post directory + landing-calculator "Unknown area" fix.** (a) New `services/pincode-sync.service.ts` runs the full live directory pull **in-process** (no queue): `fetchPage` paginates data.gov.in at 10k/page with 4× backoff (near-zero under test); `dedupeDirectory` collapses ~165k office rows to one canonical pincode each (HO>SO>BO, derives metro/remote + lat/lng); `applyDirectory` bulk-upserts in 1k batches; `runLiveSync` wraps it in a `pincodeSyncLog` (running→success/failed) + audit (`pincode.sync` / critical `pincode.sync.failed`). `startPincodeSyncScheduler` (boot hook in `index.ts`, after `listen`) schedules the next run at **00:30 IST** via `msUntilNextIST` + `setTimeout().unref()`, re-reading the `pincode.sync` settings doc each cycle so the time/enabled flag change without a restart; no-op when `DATA_GOV_IN_API_KEY` is empty or under test. (b) Manual trigger `POST /v1/admin/pincodes/sync` (gated `pincode:sync`; 202 fire-and-forget, 409 `sync_not_configured` when no key). (c) Frontend: **"Run live sync"** button (confirm dialog → `runLivePincodeSync`) on `/admin/pincodes`, CSV import demoted to secondary; the two "coming soon" notes rewritten to reflect the live nightly sync. (d) **Calculator fix:** the hero/playground `rate-calculator` captions under From/To used a tiny static `SAMPLE_PINCODES` map → showed "Unknown area" for any real pincode not in it (e.g. 302021, 110004) even though *calculate* resolved them. Now a debounced, abortable, cached `usePincodeArea` hook calls `GET /v1/public/serviceability/:pin` and shows the real `city, state`; "Unknown area" only when the API confirms `found:false`. **Tests:** +14 (`m6d.pincode-sync.test.ts` — schedule math, dedupe/classify, applyDirectory add-vs-update, mocked-fetch runLiveSync success+failure, route authz 403/409/202) → **114/114** backend green. **Live verified:** manual trigger against the real API `202 → scanned 165,627 offices → updated 19,586 pincodes in 117s, 0 failed`; Playwright confirms 302021→"Jaipur, Rajasthan", 110004→"New Delhi, Delhi", bogus 000000→"Unknown area". Web + server typecheck clean.

- **2026-07-03 (5)** — **LIVE official India Post directory imported via the user's registered data.gov.in key — 19,894 pincodes with real coordinates.** The registered key removed the sample key's 10-row clamp: fetched the fresher **"All India Pincode Directory till last month"** (resource `5c2f62fe…`, **165,627 offices, updated 2025-10-03, incl. latitude/longitude**) in 17 pages of 10k. Importer extended to accept `lat`/`lng` columns (model fields existed). Imported through the admin API in 7.2s: `scanned 19,586 → added 797 (new since the older export), updated 18,789, failed 0`. Final DB: **19,894 pincodes, 39 states/UTs, 718 metro, 1,218 remote, 18,395 with real coordinates** (110001 → New Delhi GPO @ 28.6268,77.2066 ✓). Delhi→Port Blair correctly quotes the `ne_jk` island zone (₹220.71, 5–9d). Official endpoint + resource + key fingerprint + import date recorded in the in-product **pincode sync settings**; the key lives in `server/.env` (`DATA_GOV_IN_API_KEY`) for future re-syncs — a scheduled live sync is now a small follow-up (fetch script exists: `scratchpad/fetch-indiapost-live.cjs`).
- **2026-07-03 (4)** — **Final review sweep (10 findings fixed) + REAL India Post directory imported (19,097 pincodes).** Backend **100/100**.
  - **M6/M7 adversarial review → 10 confirmed, ALL FIXED:** (1) `/admin/overview` leaked audit-log content to roles without `audit:read` → activity feed now permission-gated in the service; (2) ticket assignee accepted any ObjectId → must be active platform staff; (3) `revenue_30d` summed a 7-day window → 30; (4) revenue_series months skipped/duplicated via `setMonth` on day-29/31 → UTC day-1 anchored buckets; (5) usage series `failed` counted 4xx (double-counting 429s) → 5xx, matching the summary; (6) last-super-admin demotion write-skew → post-save recount + compensating revert; (7) CSV import: Fastify's default 1MiB bodyLimit beat the 5MB zod cap → `bodyLimit: 8MB` + client-side file-size guard with a clear error; (8) tenant detail rendered `-1` quota → "∞ / Unlimited plan"; (9) page-scoped CSV exports presented as full exports → "(this page)" disclosure + button rename; (10) pricing comparison table hardcoded figures → data-backed rows derived from live `/public/plans`.
  - **Import hardening (prereqs for real data):** RFC-4180 quoted-field CSV parsing (real office names contain commas), in-file pincode dedupe, and **bulkWrite batches** (19k rows in 6.2s over the SSH tunnel vs ~30min row-by-row).
  - **REAL DATA:** downloaded the official Department of Posts **All India Pincode Directory** (data.gov.in export, 154,823 offices; the API sample key clamps at 10 rows/req so the bulk export mirror was used), transformed to one row per pincode with metro/remote derivation (803 metro districts, 1,158 NE/J&K/island pincodes), and imported **through the product's own admin import API**: `scanned 19,097 → added 19,077, updated 20 (old seed), failed 0`. Verified live: serviceability resolves real cities (Gurgaon, Shillong, Kochi); national quotes classify correctly — Mumbai→Kolkata `metro` ₹122.91 (2-4d), Gurgaon→Shillong `ne_jk` ₹220.71 (5-9d), Kochi→Kochi `within_city` ₹75.33 (1-2d). Platform stats/status/marketing pages now show the real 19k figure.
- **2026-07-03 (3)** — **🏁 PROJECT FUNCTIONALLY COMPLETE — M6c pages + M7 public surface done. ZERO `mock-data` imports remain in the entire app. Backend 100/100. All browser E2Es PASS.**
  - **M6c pages (5-agent workflow, all 10 wired + E2E PASS):** pincodes (real stats/search; **"Sync now" = CSV import dialog** — E2E drove a real upload → 200 → run in sync-logs; fake progress bars/rollback/export/edit removed as no-endpoint deferrals), sync-logs (real runs + detail drawer), sync-settings (form built from the real settings doc), zones + rate-cards (read-only real engine pricing; editors deferred with honest notes), api-keys-audit (cross-tenant + force-revoke), audit-logs (filters + CSV), team (role change w/ guards; invites deferred), settings (per-key cards off the real settings collection), notifications (**honest platform-alerts feed** from warning+critical audit events; channel/template editors deferred).
  - **M7 public surface (3-agent workflow + E2E PASS):** `/v1/public/status` + `/v1/public/stats` endpoints (component health; **90-day uptime/latency derived from real request logs**; live pincode/state/zone stats). Status page fully live (60s refetch; fake incidents deleted → honest "No incidents reported"; per-window fake uptime chips → real uptime/latency/request chips; **honestly showed cache degraded while Docker/Redis was off**). Pricing + landing teaser + signup `?plan=` chip → `/v1/public/plans` (paise→rupees, Custom/Unlimited guards). Features + about → real zones/pincode/state stats; "nightly India Post sync" copy rewritten to honest managed-imports. Landing rate calculator untouched (was already real since M2).
  - **Ops note:** the first M7 workflow run survived a session break and raced its relaunched twin — duplicate stopped via TaskStop, files verified clean (typecheck + sweep). Two E2E pincodes + coupon cleaned from the live DB.
  - **Remaining (honest deferrals only, all visibly labeled in-product):** TOTP 2FA, admin impersonation, staff invites, India Post live auto-sync, notification channels/templates, plan creation (edit exists), zone/rate-card platform editors, invoice PDF "download all" zip.
- **2026-07-03 (2)** — **M6b (plans/coupons/billing admin) + M6c backend done; M6c pages wiring.** Backend **100/100** vitest (m6b 4/4, m6c 8/8; the single intermittent red is the known M2b log-poll flake).
  - **M6b backend:** `/v1/admin/plans` (+PATCH — audited; **price edits hit new checkouts only**, snapshots untouched, test-locked), `/v1/admin/coupons` (+POST/PATCH; value units: percent points/rupees/months at the DTO; paused coupons stop validating immediately), `/v1/admin/billing/{summary,invoices}` (real MRR/ARPU/collected/refunded/past-due/plan-mix; cross-tenant invoice list w/ company names).
  - **M6b pages (3-agent workflow):** plans (edit dialog w/ price-warning line, active-subscriber counts, deactivate-with-confirm; create removed as deferral), coupons (create/pause/resume vs real API taxonomy percent/flat/free_months; delete→pause; SAVE20 visible), billing (6 KPI cards + plan-mix + server-filtered invoice table + **refund button on paid rows** wired to the hardened endpoint). Browser E2E PASS (real plans/₹4,999, coupon created live 201 + listed, billing renders). Zero mock imports.
  - **M6c backend:** pincode master (`stats`, `search`, **CSV import as the real sync path** — upserts + PincodeSyncLog + failed-record capture; sync-logs; sync-settings via the settings doc), zones (read-only joined with live ZONE_PRICING), api-keys audit (cross-tenant list + **force-revoke** — key 401s immediately, owner notified, critical audit), platform team (roles list, role change w/ self-change + last-super-admin guards, permVersion bump), platform settings CRUD (audited), rate-cards overview (standard matrix + cross-tenant customs). 10 remaining admin pages wiring via a 5-agent workflow.
- **2026-07-03** — **M6a (admin core) built, tested, browser-verified.** Backend **88/88** vitest (`m6.admin.test.ts` 10/10).
  - **Backend:** `admin.service.ts` + `/v1/admin/*` routes (platform-gated, no requireTenant): `overview` (real MRR from active subs, tenant/signup counts, apiLog aggregates + 7-day sparklines, 12-month collected revenue from paid invoices, sync health from pincodes+syncLogs+settings, **derived alerts** — empty when healthy, recent audit activity), `usage-report` (platform series/endpoints/top-consumers, p99 via `$percentile`), `tenants` (search/filter/pagination + detail through the audited `adminRepo`), `tenants/:id/suspend|activate`, `audit-logs`, `staff`, and the cross-tenant **support queue** (`adminListTickets/Get/Reply/Update` in ticket.service — SLA policy urgent4h/high8h/med24h/low72h, agent replies notify the requester, internal notes never leak to tenants, status changes leave system markers).
  - **Suspension is now real:** company.status was previously unenforced. Suspend now revokes all tenant sessions + bumps permVersion (live tokens die instantly) and `login()` refuses suspended workspaces (`workspace_suspended`). Test-locked: works → suspend → token 401 + login 403 → activate → login 200.
  - **Frontend:** 6 pages wired off mock via a 4-agent parallel workflow (first run lost to a session limit; relaunched same script): dashboard (single `getAdminOverview`; honest "All clear" alerts state; removed fake next-sync tile), usage-reports (range select, client-derived anomalies replacing hardcoded ABUSE_FLAGS, real CSV export), users list (debounced server search/filter/pagination, suspend/activate with confirm, CSV; **impersonate + invite honestly removed** as deferrals), user detail (real subscription/usage-chart/keys/invoices/activity; "Send password reset" wired to the real forgot-password flow), tickets queue (live SLA chips, computed summary chips, server filters) + workspace (status/priority/assignee selects, Reply vs Internal-note composer tabs).
  - **Login now routes platform staff to `/admin`** (was always `/app`, causing tenant-scoped 403 noise for admins).
  - Browser E2E PASS (cross-portal loop): tenant creates ticket → admin dashboard KPIs real → tenant search → detail → queue → workspace **agent reply 201 → resolve** → tenant sees the reply + resolved status. Zero console errors. Zero mock imports in all 9 touched files.
  - **Admin gotcha fixed en route:** seeded super-admin password had drifted from `.env` (seed skips existing users) — restored via the app's own forgot/reset flow; noted in memory.
- **2026-07-02 (2)** — **Billing adversarial review → 8 confirmed findings, ALL FIXED (78/78 vitest, E2E re-PASS).** 3-lens Workflow (money math / payment state machine / authz+wiring) with adversarial verify:
  1. **Coupon over-redemption (medium):** cap was checked at validate but incremented at activation → N staged open checkouts could each capture an under-priced payment. Fix: **atomic guarded reservation at checkout** (`findOneAndUpdate` with `$expr $lt` cap filter + `$inc`); activation now only links the coupon (no `$inc`). Regression test added.
  2. **Silent refund failure (high) + 7. null-ambiguity (medium):** `createRazorpayRefund` returned null for BOTH intentional-skip and real API failure, and `refundInvoice` flipped state regardless → customer downgraded without a refund on a Razorpay outage. Fix: refund lib returns `{skipped:true}` for sim/test/unconfigured and null only on real failure; `refundInvoice` now **throws 502 before mutating** on null (retryable).
  3. **Webhook+verify double-activation race (medium) + 6. terminal-state re-activation (high):** replaced the read-check-save with an **atomic CAS claim** (`status ∈ {open,past_due} → paid`); refunded/void invoices can never be re-activated by replayed verify/webhook (regression: pay→refund→replay valid-signature verify → 400, state intact). `verifyPayment` returns the claimed doc; `markPaymentFailed` is now CAS on `open` only (late `payment.failed` can't clobber a paid invoice — regression added).
  4. **Frontend GST drift (low):** checkout dialog now mirrors backend integer-paise math (`discount_paise` from the API) — no more ±1 paise dialog/invoice mismatch at half-paise boundaries.
  5. **PDF download 401 (medium):** `downloadInvoicePdf` now does the single-flight refresh→retry like `apiFetch` (exported `refreshAccessToken` from the client).
- **2026-07-02** — **M4b (billing extras) built, tested, browser-verified — billing milestone CLOSED.** Backend **75/75** vitest (`m4b.billing.test.ts` 5/5).
  - **Coupons:** `validateCoupon` in billing.service (status/window/redemption-cap/tenant-target/plan-scope checks; percent-bps / flat-paise / free-months discount math, clamped to base) + `POST /v1/billing/coupon/validate`; checkout accepts `coupon_code` → discount line-item on the invoice, GST computed on the discounted base; `activateFromInvoice` increments `redemptionCount` + stamps `couponId`/`discountAppliedPaise` on the sub. Checkout dialog got a real coupon field (apply/remove, error display, live discount row). Demo coupon **SAVE20** (20%) seeded.
  - **Dunning:** webhook now handles `payment.failed` → invoice `past_due`, `dunning.attempts++` on the sub (kept active for grace), **dunning email** (`sendDunningEmail`) + error notification + audit.
  - **Refunds (platform):** `POST /v1/billing/invoices/:id/refund` gated `invoice:refund` (cross-tenant, no requireTenant) → real Razorpay refund API when the payment was real (`createRazorpayRefund`; simulated payments just flip state), invoice → `refunded` (new enum value), tenant dropped to Free, audit + notification. Tenant owner correctly 403s.
  - **PDF invoices:** installed **pdfkit**; `lib/invoice-pdf.ts` renders a real GST-style A4 PDF from invoice line-items; `GET /v1/billing/invoices/:id/pdf` (tenant-scoped, `invoice:read`) streams `application/pdf`; invoices page now downloads the real PDF (binary fetch with Bearer token, replaces the client-HTML fallback).
  - Browser E2E PASS: SAVE20 in checkout → −₹999.80 (exactly 20%) → upgrade → real `INV-*.pdf` download (application/pdf), zero console errors. Demo tenant reset to Free; SAVE20 redemptions reset. Adversarial review workflow over the money paths launched (results pending).
- **2026-07-01 (7)** — **M4 Razorpay billing (core) built, tested, browser-verified.** Keys stored in `server/.env` (`RAZORPAY_KEY_ID/SECRET/WEBHOOK_SECRET`) — the provided test keys turned out to be **valid** (real order API works; only the interactive card modal needs a human). New **Invoice model** (scoped) + `lib/razorpay.ts` (order create via REST, `verifyPaymentSignature` = HMAC(order|payment, secret), `verifyWebhookSignature`, dev `simulatePayment`) + `billing.service.ts` (`createCheckout` → Razorpay order + OPEN invoice; `verifyPayment` → signature-check → `activateFromInvoice` upgrades the sub + marks invoice paid + emits notification; `handleWebhookEvent` = source of truth; `cancelSubscription` = cancel-at-period-end; `devCompleteCheckout` simulates in dev) + `billing.routes.ts` (`/v1/billing/{plans,invoices,checkout,verify,dev-complete,cancel,webhook}`, gated `billing:write`/`invoice:read`; raw-body capture moved to the app-level JSON parser for webhook HMAC). Wired `/app/billing` (plan+quota+invoices+real cancel; honest Razorpay note, no fake stored card), `/app/billing/plans` (interval toggle → checkout: **dev simulates, prod opens the real hosted modal**; free-downgrade → cancel), `/app/billing/invoices` (filters + client-side printable invoice download). **In dev the flow always simulates the payment** (the modal can't be driven headlessly); prod uses the real modal with zero code change. Backend **70/70** vitest (`m4.billing.test.ts` 6/6: order+open-invoice, dev-complete activation, bad/good signature, signed webhook + reject, cancel, isolation). Browser E2E PASS (Free → upgrade → Growth + paid invoice → cancel). **Deferred to M4b:** coupons, server-rendered PDF, dunning emails, admin refunds. **Op note:** SSH tunnel dropped after the E2E → demo tenant left on Growth (harmless real data); reset to Free pending tunnel.
- **2026-07-01 (6)** — **M5 (support · notifications · profile+security) built, tested, browser-verified.** Understand phase via a 4-agent parallel Workflow that produced implementation specs. Backend **57/57** vitest (+6 tickets, +5 notifications, +5 account).
  - **M5a Support tickets:** `ticket.service.ts` + `tickets.routes.ts` (`/v1/tickets` list/get/create/reply, gated `ticket:create`, reads = authenticate+requireTenant; ticketNumber `PP-YYYY-NNNNNN` is the routable id; reply reopens resolved/closed). Aligned ticket-model enums (category/priority) to the frontend vocabulary. Wired `/app/support` list + `[id]` detail (new `ticket-detail.tsx` client) + `ticket-thread.tsx` (real reply mutation) + `new` form → create. Browser E2E PASS (create→list→thread→reply). Requester/assignee names resolved via user lookup; company name from CompanyModel.
  - **M5b Notifications:** `notification.service.ts` (per-user `recipientId` scope, 30d TTL) + `createNotification` helper **emitted from real events** (key create/revoke, ticket create) so the feed is organic, not seeded. `/v1/notifications` list + `/:id/read` + `/mark-all-read` + `/unread-count` (gated authenticate). Wired `/app/notifications` off mock with optimistic mark-read. Browser E2E PASS (2 organic notifs → mark-one → mark-all).
  - **M5c-1 Profile + Security:** `PATCH /v1/auth/profile` (name/locale/timezone), `POST /v1/auth/change-password` (verify current, bump permVersion, keep current session + revoke others), enhanced `GET /v1/auth/sessions` (real `current` flag + UA→device/browser), `POST /v1/auth/sessions/:id/revoke` + `/revoke-others` (+ session.service `revokeSessionById`/`revokeOtherSessions`). Wired `/app/settings` (profile off mock; email/company read-only) + `/app/settings/security` (password + sessions live; **2FA = honest "Coming soon"**; delete-account files a real support ticket). Browser E2E PASS (profile persists, sessions show "This device", pw strength+mismatch). **Note:** did NOT change the demo password in E2E (protects login creds).
  - **M5 adversarial review (3-dimension Workflow) → 6 findings fixed:** (1) **`login()` now rejects non-active users** — closes an invited/suspended account obtaining a session (incl. the forgot/reset back-door that skipped `acceptInvite`); regression test added. (2) **Password policy unified to 12 chars** across route schemas (signup/reset/accept-invite/change-password → `AUTH.minPasswordLength`) + all client gates (signup/reset/security/accept-invite). (3) ticket status filter enum now includes `on_hold`. (4)+(5) client password gates raised to 12. (6) **topbar unread dot wired to `/v1/notifications/unread-count`** (portal only; hidden at 0) — browser-verified. Server **64/64** vitest.
  - **M5c-2 Team:** `member.service.ts` + `members.routes.ts` (`/v1/members` list/invite/role/remove, gated `member:read`/`member:write`/`member:role` — owner-only; seat cap from plan `maxTeamMembers`; last-owner + self-action guards; remove hard-deletes to free the email). Full **invite → accept flow**: invite creates an `invited` user + emails a set-password link (reuses `passwordResetToken*`), `POST /v1/auth/accept-invite` (public) sets name+password+active, new `/(auth)/accept-invite` page. Reconciled the team page's role vocabulary to the real backend roles (owner/developer/member). Wired `/app/settings/team` off mock. Backend 6/6; browser E2E PASS (invite → accept in a fresh context → new member logs in → owner sees active → role change → remove). **Server total 63/63 vitest.** M5 COMPLETE.
- **2026-07-01 (5)** — **M3 complete + verified (webhooks + rate-cards).**
  - **Webhooks:** new `webhook.service.ts` (CRUD + **real signed outbound delivery** — HMAC `X-Postpin-Signature` `t=…,v1=…`, 8s timeout, SSRF guard active in prod, rolling success-rate, cascade-delete deliveries) + `webhooks.routes.ts` (`/v1/webhooks` CRUD, `/:id/test`, `/:id/roll-secret`, `/deliveries`, `/deliveries/:id/replay`, gated `webhook:write`). Wired `/app/webhooks` to react-query (create → one-time secret dialog, send-test, pause/resume, delete, replay). Added scoped-repo `deleteMany`/`findByIdAndDelete`.
  - **Rate-cards:** new `rate-card.service.ts` — read surface synthesizes the **Standard card from the live engine pricing** (ZONE_PRICING → per-zone/slab freight; surcharges mirror engine DEFAULTS: fuel 12 %, GST 18 %, COD ₹35+1.5 %) so the page matches real quotes, plus custom-card CRUD (`/v1/rate-cards`, soft-delete, gated `ratecard:read`/`ratecard:write`) + real zone map. Wired read-only `/app/rate-cards` off mock; zone tile now shows real `tier`.
  - **Tests:** 41/41 server (was 30) — `m3.webhooks.test.ts` 6/6 (incl. signed-delivery + signature verify against one-time secret + replay + failed-delivery success-rate) and `m3.ratecards.test.ts` 5/5 (engine-consistent slabs, CRUD persistence, Standard-card guards, tenant isolation). Browser E2E: webhooks full lifecycle PASS; rate-cards read surface PASS (₹46 local freight, GST 18 %/fuel 12 %, zones). Zero console errors; both pages import **no** `mock-data`.
- **2026-07-01 (4)** — **Keys-detail residual fixed + verified.** Backend `GET /v1/keys/:id` + per-key `GET /v1/usage/logs?key_id=`; `app/keys/[id]` rewritten to react-query (real key fetch, domain PATCH, revoke, per-key logs) — was a broken flow with real key ids. Browser-verified: detail renders, domain add persists across reload. `/pricing` left as an **accepted cosmetic residual** (mock plan values are identical to seeded plans; marketing page best kept static/SSG; `/v1/public/plans` exists if we later want it dynamic). Server restarted after a tunnel drop left mongoose disconnected (Windows: `TaskStop` doesn't kill the child node → had to kill port-4000 owner).
- **2026-07-01 (3)** — **M2b complete + verified.** `/v1/subscription` + `/v1/usage/{summary,series,logs,endpoints,status}` (apiLogs aggregation; enriched log `detail`). Dashboard + usage pages rewritten to react-query off mock; real client-side CSV export. 30/30 vitest; browser-verified both pages render live data (Free plan 3/1000; usage /v1/rates/calculate 3 calls 93ms). Fixed empty-body content-type parser earlier this session.
- **2026-07-01 (2)** — **M2 VERIFIED end-to-end.** Restarted api+web (tunnel + Redis:6380 up), Playwright M2 E2E **6/6**: anon landing quote via public API; login; create key via UI (one-time secret); keyed rate call ₹196.66; revoke→401; no-key→401. Added empty-body JSON content-type parser (POST `/keys/:id/revoke` no longer 400s). Demo tenant is Free (maxApiKeys 1) → E2E create+revoke nets 0 active, re-runnable.
- **2026-07-01** — **M2 backend complete + tested (28/28).** Server rate engine (ports `shipping.ts` → paise, seeded zones/pincodes); public endpoints `/v1/public/{rates/calculate,serviceability,pincodes,plans}` (IP rate-limited); API keys `pp_live_/pp_test_` (HMAC+pepper) create/rotate/revoke; key-auth middleware + keyed `/v1/rates/calculate` + Redis rate-limit/quota (fail-open) + apiLogs. Redis in dedicated Docker container on **6380** (isolated from other apps). Frontend: rate-calculator → public API, `app/keys` fully wired (react-query CRUD + one-time secret), `/legal/sla` fixed. Web + server typecheck clean. Live browser E2E pending SSH tunnel. NOTE: Windows Bash can't reach Docker engine pipe — use PowerShell for `docker`.
- **2026-06-30 (4)** — **M1 COMPLETE & VERIFIED.** Live `npm run seed` ran against `postpin` (super-admin + FlipMart demo tenant). Both servers up (api :4000 db-connected, web :3000). **Playwright E2E 6/6 PASS**: logged-out `/app`→/login redirect; login as demo owner → dashboard shows real "Aarav Sharma" + Free plan/usage from `/me`; logout → /login; signup new tenant → check-inbox; verify via token (`/v1/auth/_dev/emails`) → verified; login as new verified user → dashboard. Fixed two test-harness issues (React-controlled-input + hydration race → `pressSequentially` + verify-retry). Next: M2.
- **2026-06-30 (3)** — M1 frontend wired: `src/lib/api` client (credentials, single-flight 401→refresh, CSRF, error envelope) + `<SessionProvider>`/`<QueryProvider>` + `QueryBoundary`; all 5 `(auth)` pages on real `/v1/auth/*`; **OAuth buttons removed**; portal/admin layouts off `currentUser` → client shells with `useSession` + real sign-out. Web typecheck ✅; dev render smoke ✅ (/, /login, /signup, /pricing → 200). Gating is client-side (no edge middleware — separate-origin + in-memory token). Live seed + browser E2E pending SSH tunnel.
- **2026-06-30 (2)** — M1 backend complete: seed (permissions/roles/plans→paise/zones/pincodes/super-admin/demo tenant) + custom auth (signup/verify/login/refresh-rotation/logout/forgot/reset/me) + Argon2id + EdDSA + non-transactional onboarding + middleware (authenticate/tenant/authorize/csrf) + auth routes + `/v1/_demo` isolation route. **21/21 vitest green** incl. HTTP tenant isolation + refresh-reuse family revoke + RBAC + lockout. Fixed Fastify ALS context propagation (onRequest `startContext` + mutate). Live seed pending SSH tunnel; frontend wiring next.
- **2026-06-30 (1)** — M0 complete: server scaffold + 22 models (companyId-leading indexes, `assertScoped`) + RBAC contracts + tenancy primitives (scoped-repo/admin-repo/context/audit). Test gate 10/10 green (vitest + mongodb-memory-server). Confirmed dedicated `postpin` db (standalone, no transactions). Started M1.
